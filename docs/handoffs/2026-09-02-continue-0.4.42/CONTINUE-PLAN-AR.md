# خطة الاستكمال — بعد `f059bc6` (ليلة 2026-09-02)

**أرشيف المحادثة:** نفس المجلد [`README.md`](./README.md)  
**آخر commit:** `f059bc6`

---

## P0 — على الجهاز الآخر فوراً

1. `git pull origin main`
2. تأكد أن Vercel نشر `f059bc6` (أو أحدث) على `https://r.bhd-om.com`
3. دخّل كمالك وتحقق:
   - `/ar/owner/stays` — ترجمة + حجوزات بعقار
   - `/ar/owner/stays/bookings` — عمود العقار/الوحدة + روابط
   - `/ar/owner/stays/calendar` — أيام **محجوز** للحجوزات المدفوعة  
     (للحجوزات القديمة: افتح تقويم العمليات مرة واحدة للمزامنة)

---

## P1 — تحقق مسار الضيف

| خطوة | متوقع |
| --- | --- |
| تأكيد بعد الدفع | ترحيب بالاسم + المبلغ + المرجع؛ شعار زاوية؛ بدون تلميح Booking.com |
| إيصال الدفع / تأكيد الحجز | `?doc=payment` و `?doc=confirmation` — شعار مرة واحدة فقط؛ بدون هيدر الموقع |
| ظهور الحجز للمالك | نفس المرجع في الرئيسية / الحجوزات اليومية / التقويم |

---

## P2 — يدوي / تشغيلي (ما زال معلّقاً)

| بند | ملاحظة |
| --- | --- |
| نشر وحدات Al Noor المتبقية | معالج المالك + جلسة / DB |
| SMTP لعامل إيميل التأكيد | تحقق على الإنتاج بعد الدفع |
| تقويم عام Nest بدون أقفال | اختياري: تضمين locks أو الاعتماد على `inventory_days` |
| ربط إيصالات الإقامة بالمحاسبة/AR | غير مطلوب الآن — الإيجار ≠ الإقامة |

---

## P3 — ترجمة متبقية (اختياري)

- نصوص إنجليزية ثابتة في kickers العمليات / معالج الإعداد
- أي تسميات حالة جديدة → أضفها في `apps/web/src/lib/ui-labels.ts` لا تنسخ نصاً صلباً

---

## تم في الكود (مرجع سريع)

- [x] ترحيب شخصي + تخطيط تأكيد + مستندات وازن
- [x] شعار إيصال مرة واحدة + إخفاء كروم الصفحة
- [x] قائمة حجوزات المالك من Neon (+ تقويم + inventory-days)
- [x] `ui-labels` ثنائي اللغة للحالات/النشاط
- [x] اسم العقار على الحجوزات + أيام محجوزة بعد الدفع

# معالج إضافة عقار — BHD R

**المسار:** `/{ar|en}/{owner|developer}/properties/new`  
**المكوّن:** `apps/web/src/components/property-wizard.tsx`

## مراحل المعالج (1–7)

| # | المحتوى |
|---|--------|
| 1 | بيانات العقار + رابط خرائط جوجل |
| 2 | الوحدات (رمز تلقائي، طابق/غرف/حمامات قوائم) |
| 3 | التشغيل والمرافق (بدون عدد طوابق) |
| 4 | الملكية + مرفقات خاصة اختيارية (سند / كروكي / بطاقة مالك) |
| 5 | الصور (صورتان على الأقل) |
| 6 | الوصف والترجمة |
| 7 | عرض العقار بأسلوب منصات الحجز ثم الحفظ |

- الضغط على رقم مرحلة **سابقة** يرجع إليها.
- الانتقال لمرحلة لاحقة ممنوع حتى تكتمل الحالية (تحقق + قائمة نواقص).

## التحقق المرئي

- الحقول الإلزامية الفارغة: إطار أحمر (`.field--missing`)
- بعد التعبئة: إطار أخضر (`.field--ok`)
- عند «التالي» مع نقص: تنبيه + قائمة الحقول

## موقع خرائط جوجل

- حقل إلزامي: رابط موقع العقار من Google Maps
- يُستخرج خط العرض/الطول تلقائياً وتُعرض معاينة خريطة مضمّنة
- تُحفظ الإحداثيات مع العنوان؛ الرابط يُذكر في ملاحظات الملف

## اسم واحد للعقار

- الاسم (عربي/إنجليزي) في مرحلة بيانات العقار فقط
- الوحدات ترث الاسم؛ متعدد الوحدات يُلحق رمز الوحدة `(U-01)` تلقائياً

## غرف/حمامات/طابق

- قوائم منسدلة فارغة إلزامية (بدون قيم افتراضية)

## الوصف والترجمة

- توليد نص احترافي من البيانات: `apps/web/src/lib/property-listing-copy.ts`
- ترجمة فورية AR↔EN عبر `POST /api/translate` (MyMemory من الخادم) مع احتياطي معجم عُماني للعناوين
- أزرار `AR → EN` / `EN → AR` بجانب الاسم والوصف

## تخطيط ثنائي اللغة والمرافق

- الاسم العربي والإنجليزي (والوصف) في صف واحد عبر `.bilingual-pair` — ليس فوق بعض
- المرافق كبطاقات بأيقونات `.amenity-chip`

## صورة الغلاف

تُرفع أولاً بـ `position: 0` عبر `POST /v1/media/:id/complete` (`position` اختياري). الغلاف = أصغر `unit_media.position`.

## الترقيم المتسلسل

- عقارات: `BHD-{year}-PRP-{R|S|I}-{NNNN}` عند الإنشاء  
  جداول: `properties.serial_number` + `property_sequences` (هجرة `0012_property_serials.sql`)
- عقود: موجودة مسبقاً `CTR-{year}-{NNNNNN}` عبر `contract_sequences`
- مستخدمون: نفس نمط bhd-om `USR-…` يُخطَّط لاحقاً عند توحيد الترقيم عبر المنصة

## أين تظهر الوحدة للعامة؟

عند تفعيل «عرض الوحدة عند توفرها»: الصفحة الرئيسية `/` وصفحة `/properties` للزوّار بعد أن تصبح متاحة ومنشورة — وليس داخل بوابة المالك فقط.

## تجربة الواجهة

- انتقال المراحل كشرائح (slide) مع دعم RTL و`prefers-reduced-motion`
- شريط تقدّم دائري أنيق بدل صناديق المراحل الثقيلة
- بطاقة زجاجية خفيفة، حقول أوضح، أزرار دائرية في تذييل لاصق
- الاسم/الوصف عربي‖إنجليزي جنباً إلى جنب؛ المرافق بأيقونات

## إصلاح بناء Vercel (0.2.20)

`exactOptionalPropertyTypes`: تمرير `area` / `coverUrl` فقط عند وجود قيمة.

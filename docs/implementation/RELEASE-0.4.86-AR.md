# إصدار 0.4.86 — لا شاشة بيضاء ولا 404 كاذب للوحدات

**تاريخ:** 2026-09-11

## المشكلة

1. عند التحديث/التنقل تظهر هيدر+فوتر وشاشة بيضاء 20–40 ثانية.  
2. بعض الوحدات المنشورة تظهر 404 «حُجزت أو أُجرت» خطأً.

## السبب

- `loading.tsx` على مستوى اللغة كان فارغاً، وصفحة الوحدة تنتظر Neon بلا هيكل.  
- مهلة showcase (8 ث) كانت تُحوَّل إلى `notFound()` حتى لو وُجدت بيانات الوحدة.

## الإصلاح

- هيكل تحميل لـ `/units/[id]` و`/properties/[id]`  
- التمييز بين: موجودة / غير موجودة / مهلة مؤقتة  
- عند مهلة المبنى: عرض تفاصيل الوحدة المبسّطة بدل 404  
- heal الكتالوج في الخلفية دون حجب الصفحة  
- نص 404 أوضح

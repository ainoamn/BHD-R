# BHD R 0.4.77 — تسريع صفحة المحاسبة

## السبب
`/ar/owner/accounting` كانت تنتظر استيقاظ Nest على Render (غالباً دقيقة–دقيقتين) عبر `healthz` قبل إظهار صفوف Neon الموجودة أصلاً.

## الإصلاح
1. المحاسبة: قراءة مدفوعات/حجوزات الإقامة من Neon أولاً دائماً.
2. إثراء Nest اختياري بسقف 1.2 ثانية فقط.
3. `probeNestHealthz` بـ Promise.race صارم (لا يعتمد على AbortSignal وحده).
4. أعمدة التاريخ/البيان تقبل `createdAt` / `memo` كاحتياطي.

## تحقق
افتح المحاسبة بعد النشر — يفترض ظهور الصفوف خلال ثوانٍ دون انتظار Nest.

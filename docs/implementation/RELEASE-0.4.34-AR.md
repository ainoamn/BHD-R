# الإصدار 0.4.34 — إعداد الإقامة: جدول وحدات كالمحفظة + ترجمة + معاينة عامة

**التاريخ:** 2026-09-02  
**الإنتاج:** https://r.bhd-om.com  
**صفحة الإعداد:** https://r.bhd-om.com/ar/owner/stays/setup?propertyId=d0840631-207d-477a-853a-043572d49240

## الملخص

1. **جدول الوحدات** بنفس أسلوب محفظة العقارات: صورة + رمز + اسم + غرف + حمّامات + حالة + اختيار.
2. **اسم نوع الوحدة**: ترجمة تلقائية حقيقية AR↔EN عبر `/api/translate`.
3. **السياسات**: مربعان (عربي / إنجليزي) مع ترجمة تلقائية بينهما.
4. **حذف التعليمات** من المعالج (ستأتي لاحقاً من تقييمات الضيوف).
5. **المراجعة والنشر**: معاينة كاملة بنفس مكوّن الصفحة العامة (`StayPublicShowcase`).

## تحقق

- [ ] جدول الوحدات يظهر بصورة مصغّرة وحالة `ops-status`
- [ ] ترجمة اسم النوع والسياسات تعمل بين اللغتين
- [ ] لا يظهر قسم التعليمات
- [ ] خطوة المراجعة تعرض الصفحة كما للجمهور

# مزامنة حزمة BHD-R complete 0.1.6 — 24 أغسطس 2026

## مرجع المنتج للحزمة

الوصف الوظيفي لوحدات التشغيل (عقارات، طلبات، عقود، محاسبة، تقارير…) موثَّق في
[`OPERATIONS_SUITE_AR.md`](./OPERATIONS_SUITE_AR.md) — هذه هي وثيقة الحزمة المعتمدة للاطلاع.

## المصدر

- المجلد: `BHD-R-complete-0.1.6/BHD-R`
- الأرشيف: `BHD-R-complete-0.1.6.zip`
- الهدف: مستودع `ainoamn/BHD-R` (المجلد التشغيلي `BHD-R`)

## نتيجة التحقق

تمت مقارنة محتوى الحزمة الكاملة مع المستودع عبر تجزئة SHA-256 لكل الملفات المصدرية (مع استثناء `node_modules` / `.git` / مخرجات البناء).

| البند                                              | النتيجة                              |
| -------------------------------------------------- | ------------------------------------ |
| ملفات الحزمة مقابل المستودع                        | متطابقة (363 ملفًا)                  |
| `package.json` / `CHANGELOG` / `pnpm-lock.yaml`    | متطابقة                              |
| بوابة المالك للجوال (`portal-nav` + `globals.css`) | متطابقة مع الحزمة                    |
| فرق وحيد قبل المزامنة                              | `.gitignore` (فروق سطور CRLF/LF فقط) |

ثم نُسخت الحزمة فوق المستودع بـ robocopy مع الإبقاء على الأسرار المحلية (`.env*` / `.vercel`) ومجلدات الأدوات.

## النشر

- الإصدار الموثَّق: **0.1.6**
- الفرع: `main`
- الموقع الحي: https://bhd-r-api-phi.vercel.app

## مزامنة المجلد الأب (نفس اليوم)

راجع [`PARENT_FOLDER_SYNC_AR.md`](./PARENT_FOLDER_SYNC_AR.md) لخريطة كل مجلد/أرشيف مجاور (`screenshots`، `outputs`، `generated-assets`) إلى مساره داخل المستودع.

## ملاحظة

`/ar/owner` بدون جلسة يحوّل إلى الدخول الموحّد. بعد تسجيل الدخول تظهر لوحة المالك بشريط الجوال والقائمة الجانبية المنزلقة.

/**
 * Ops section UI is owned by PortalMainSlot (persistent shell) so Next.js
 * route transitions never blank the main pane. This page is a URL anchor only.
 */
export default function Page() {
  return null;
}

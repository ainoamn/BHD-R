# Security Policy

## الإبلاغ عن ثغرة

لا تفتح Issue عامة لثغرة أو بيانات حساسة. استخدم GitHub Private Vulnerability Reporting لهذا المستودع، أو قناة الأمن الخاصة التي يحددها مالك BHD. أرسل وصف الأثر، خطوات إعادة آمنة، الإصدار/commit وأي proof محدود. لا ترسل أسراراً حقيقية أو بيانات مستأجرين.

سنؤكد الاستلام حسب شدة البلاغ، نفرز الأثر، نجهز إصلاحاً واختبار regression، ثم ننسق الإفصاح. لا يوجد bug bounty ضمني ما لم يعلن المالك برنامجاً مستقلاً.

## النطاق المسؤول

مسموح في بيئة محلية/اختبارية تملكها: مراجعة الكود واختبارات آمنة بحساباتك. غير مسموح دون تفويض مكتوب: اختبار الإنتاج، social engineering، DoS، الوصول لبيانات غيرك، استخراج أسرار، أو تعديل/حذف بيانات.

## الإصدارات

قبل الإطلاق العام، تُنشر سياسة إصدارات مدعومة. حتى ذلك الوقت يتلقى فرع `main` الإصلاحات، ولا يعد commit غير موسوم إصداراً إنتاجياً. راجع [ضوابط الأمن](./docs/SECURITY_CONTROLS.md) و[نموذج التهديد](./docs/THREAT_MODEL.md).

لا يمكن ضمان انعدام الثغرات 100%. المشروع يستخدم مراجعة، اختبارات، CodeQL، Trivy، gitleaks، dependency audit وSBOM لتقليل الخطر وإثبات الإصلاحات.
